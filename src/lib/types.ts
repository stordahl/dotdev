export type Post = {
	title: string;
	slug: string;
	description: string;
	date: string;
	published: boolean;
};

export type Sketch = {
  title: string;
  slug: string;
  date: string;
  published: string;
}
